region: us-east-1

function_name: opsdx-behavior-monitor
handler: service.handler
description: Analyze OpsDX logs for behavior patterns

# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
aws_access_key_id:
aws_secret_access_key:

# dist_directory: dist
# timeout: 15
# memory_size: 512
