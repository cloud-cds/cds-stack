---
platform: linux

image_resource:
  type: docker-image
  source:
    AWS_REPO:
    AWS_ID:
    AWS_KEY:
    IMAGE_TAG:

inputs:
- name: dashan-etl-pull-request

run:
  path: dashan-etl-pull-request/ci/dummy-task.sh
