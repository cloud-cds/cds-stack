from dashan_core.src.ews_client.client import DataFrameFactory, DataFrame


data_id = '' # Untrained, unfiltered frame to load
model_id = '' # Model to evaluate against, lamdas from here
score_id = '' # score to produce
eval_id = '' #evaluation to produce

# Try to only write the eval to avoid confusion

#% modle, and scalr
